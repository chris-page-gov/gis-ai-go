- Record the independently replayed, path-free protected-main projection for the
  exact-five loopback HTTP preflight while retaining the private capture locally and
  leaving capability, remote-host, provider, registration, activation, deployment
  and release claims open.
