- Add a dedicated provider-free local candidate that a clean clone can start on
  IPv4 loopback and use from an independent MCP client. It exposes only the
  immutable exact-five read-only assembly and three governed resources, keeps
  evidence in owner-only session state, and cannot make provider network calls. A
  deterministic in-memory HTTP `503` drives `data.query` through the exact
  byte-verified approved T04 cache, so its warning and receipt record
  `read-approved-provider-cache` rather than a successful live-provider execution.
  Fixed lifecycle fields report zero provider egress and the exact local observation
  and data source without changing hash-bound historical receipt schemas.
  Generic and production entrypoints, registration, deployment and the supported
  `v0.2.0` release boundary remain unchanged.
