- Record the independently verified, path-free Claude Code `2.1.241` MCP
  `2026-07-28` STDIO transport-readiness result from exact protected `main`, while
  preserving raw captures locally and keeping capability, provider access,
  activation, deployment and release explicitly unclaimed.
