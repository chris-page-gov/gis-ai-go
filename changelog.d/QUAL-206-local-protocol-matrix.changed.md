- Add a closed repository-local MCP 2026-07-28 protocol source matrix for the
  pinned official client and independent HTTP/STDIO transcripts. A test-only
  real-process fixture now exercises the unregistered exact-five assembly,
  catalogue and evidence resources, structured/plain-text parity, cancellation,
  unsupported traffic and all seven suspension scenarios through operating-system
  pipes with an injected zero-network provider fixture. Assurance verifies the
  exact runtime-base Git blobs and current subprocess sources; the matrix records
  no test-runner, live-host, activation, deployment or release outcome.
