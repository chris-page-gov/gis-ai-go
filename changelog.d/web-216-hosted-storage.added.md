Add an inactive, bounded CPIH transaction model, D1-compatible compare-and-swap
adapter and asynchronous hosted capture application for WEB-216. Cover complete
snapshot verification, same-key recovery, capacity-aware readiness and uncertain
write/cancellation outcomes with real SQLite tests. Document separate hosting,
retention, rollback and ingress acceptance still required; existing local tools,
Site, provider controls and supported release remain unchanged.
