- Add provider-neutral deployment, remote HTTPS and later live-provider admission
  contracts plus auditable filesystem, checkpoint and restoration tooling, while
  keeping cloud provisioning, spend, live calls, registration and release blocked.
