- Validate every pending changelog fragment and generate a deterministic `v0.2.0`
  release preview without consuming fragments, changing versions, setting a release
  date or claiming deployment evidence.
