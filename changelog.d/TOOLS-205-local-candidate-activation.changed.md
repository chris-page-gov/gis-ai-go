Activate the exact-five candidate only in the fixed local container entrypoint. A
closed builder derives direct HTTP, MCP HTTP, readiness and OpenAPI from one branded,
unregistered assembly, while the image admits and verifies only the approved T04
cache record. A bounded no-egress lease now reserves provider concurrency and
first-attempt capacity before immutable ownership, and exhausted local claim
capacity blocks readiness without hiding recovery routes. Generic entrypoints and
production registration remain blocked; this change makes no provider call,
deployment, registry publication or release claim.

Run the reference-runtime interoperability build before the remaining parallel
observer files so a generated-runtime writer cannot race a stable-file hash check.
