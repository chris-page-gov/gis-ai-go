{
  "name": "@gis-ai-go/webmcp-explorer",
  "version": "0.1.0",
  "private": true,
  "description": "Experimental page-scoped WebMCP Explorer for GIS AI GO",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@gis-ai-go/contracts": "workspace:*"
  },
  "scripts": {
    "prepare:data": "node scripts/prepare-data.mjs",
    "build": "pnpm --filter @gis-ai-go/contracts run build && pnpm run prepare:data && vite build && pnpm run check:dist",
    "check:dist": "node scripts/check-dist.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test:build": "node --test test/build/*.test.mjs",
    "test:unit": "pnpm --filter @gis-ai-go/contracts run build && pnpm run test:build && vitest run",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "jsdom": "30.0.1",
    "vite": "8.2.1",
    "vitest": "4.1.11"
  }
}
