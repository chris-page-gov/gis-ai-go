[project]
name = "gis-ai-go"
version = "0.0.0"
description = "Governed geospatial knowledge and action for people, systems and AI agents"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "jsonschema==4.26.0",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["services/geo-execution"]
