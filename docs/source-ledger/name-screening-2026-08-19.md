# GIS AI GO point-in-time name screen

Checked on 19 August 2026 before local repository creation. This is an exact-name
collision screen, not a reservation, availability guarantee, legal opinion or trade
mark clearance.

## Repository name

The [GitHub repository-name query](https://github.com/search?q=gis-ai-go+in%3Aname&type=repositories)
returned 2 tokenised matches. Neither was an exact `gis-ai-go` repository:

- `StarPlatinumStudio/-AI-Google-Map-GIS-`;
- `abuaziza404-tech/BOUH_GOLD_PRO_ULTRA_v12_3_1_SOVEREIGN_GIS_AI_FIXED.zip`.

The direct GitHub API check for `chris-page-gov/gis-ai-go` returned HTTP 404. That
means the target did not exist when checked; it does not reserve it.

## MCP server identity

The official MCP Registry returned zero records for both exact searches:

- [`gis-ai-go`](https://registry.modelcontextprotocol.io/v0.1/servers?search=gis-ai-go&limit=100);
- [`io.github.chris-page-gov/gis-ai-go`](https://registry.modelcontextprotocol.io/v0.1/servers?search=io.github.chris-page-gov%2Fgis-ai-go&limit=100).

The proposed full identifier remains provisional because no server is implemented or
registered during Stage 0.

## Package names

Exact API lookups returned HTTP 404 for both the
[PyPI name](https://pypi.org/pypi/gis-ai-go/json) and
[npm name](https://registry.npmjs.org/gis-ai-go).

## Close-name caution

Do not collapse the display name to `GISAI` or the slug to `gisaigo`. Those forms
have adjacent uses and are harder to distinguish. Before any external launch, repeat
the technical namespace checks and complete appropriate UKIPO, EUIPO and WIPO
similarity searches for the relevant software and service classes.
